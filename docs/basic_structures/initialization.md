# Инициализация

```
// Короткое объявление
x := 42

// Создание с new
ptr := new(int)

// Создание с make (для slice, map, chan)
s := make([]int, 10)
m := make(map[string]int)
```

## Как это работает?